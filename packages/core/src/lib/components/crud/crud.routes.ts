import { Routes } from '@angular/router';
import { LegacyCrudWrapperComponent } from '../legacy-crud-wrapper/legacy-crud-wrapper.component';

export default [
    {
        path     : '',
        component: LegacyCrudWrapperComponent,
    },
] as Routes;